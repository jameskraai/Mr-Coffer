---
  - name: Download NodeJS Setup Script
    get_url:
      url: "{{ node.setup_script }}"
      dest: /tmp
      mode: 0655

  - name: Set up NodeJS
    become: yes
    command: /tmp/setup_4.x

  - name: Install NodeJS
    become: yes
    apt: pkg=nodejs state=latest